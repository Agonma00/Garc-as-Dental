import React from 'react';

function Footer() {
    return (
        <footer className="custom-footer" style={{zIndex: 0}}>
            {/* Contenido del footer */}
        </footer>
    );
}

export default Footer;
